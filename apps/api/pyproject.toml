[project]
name = "tripwire-api"
version = "0.1.0"
description = "TripWire defense engine — Azure-native prompt injection defense for agentic systems"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
