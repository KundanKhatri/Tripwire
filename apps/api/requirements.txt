# Lean runtime — everything the defense engine needs to run the live pipeline.
# DB + telemetry extras live in requirements-prod.txt (added when Cosmos/App Insights wire in).
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
httpx==0.28.1
python-jose[cryptography]==3.3.0
orjson==3.10.12
pytest==8.3.4
pytest-asyncio==0.25.0
eval_type_backport==0.2.0
